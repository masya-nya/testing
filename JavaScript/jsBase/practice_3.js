'use strict'
/*
let browser = 'chrome'

if (browser === 'edge') {
    console.log("You've got the Edge!")
} else if (browser === 'chrome'
    || browser === 'firefox'
    || browser === 'safari'
    || browser === 'Opera') {
    console.log('Okay we support these browsers too')
} else {
    console.log('We hope that this page looks ok!')
}

const number = 0
switch (number) {
    case 0:
        console.log('Вы ввели 0')
        break
    case 1:
        console.log('Вы ввели 1')
        break
    case 2:
    case 3:
        console.log('Вы ввели 2 или 3')
        break
    default:
        console.log('Что вы вообще ввели?')
}
*/
/*
function sayHi() {
    console.log('Hi')
}
const sayHello = function () {
    console.log('Hello')
}
const welcome = () => {
    console.log('Welcome')
}
let one = sayHi
let two = sayHello
let three = welcome
sayHi()
sayHello()
welcome()
one()
two()
three()
function ask(question, yes, no) {
    if (confirm(question)) yes()
    else no()
}

ask('Хотите ли вы продолжить?',
    () => { alert('Вы согласны!') },
    () => { alert('Вы не согласны!') })
*/
let users = {
    name: 'Максим',
    sizes: {
        height: '187',
        width: '50'
    }
}